import { Button } from "react-bootstrap";

const FormdAddServices = () => {
  return (
    <>
      <input type="text" placeholder="Ingresa un servicio" />
      <Button variant="primary">Agregar</Button>
    </>
  );
};
export default FormdAddServices;
